# VendingPro - Website with Retell AI Integration

Веб-сайт с интеграцией Retell AI для чата и голосовых звонков.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` (или добавьте переменные в Vercel Dashboard):

```env
RETELL_API_KEY=your_retell_api_key_here
```

**Где получить API ключ:**
1. Зайдите на [Retell AI Dashboard](https://dashboard.retellai.com)
2. Перейдите в раздел "Keys"
3. Скопируйте ваш API Key

### 3. Настройка агентов

В файле `index.html` обновите следующие параметры:

- **Для чата** (строка 14): `data-agent-id="ваш_чат_агент_id"`
- **Для звонков** (строка 405): `agentId: 'ваш_голосовой_агент_id'`

**Важно:** 
- Для чата нужен **чат-агент**
- Для звонков нужен **голосовой агент** (не чат-агент!)

### 4. Локальная разработка

**Для статического сайта (без API endpoints):**
```bash
npm run dev
```

Проект будет доступен на `http://localhost:3000`

**Примечание:** API endpoints (`/api/create-web-call`) будут работать только после деплоя на Vercel. Для полной локальной разработки с API используйте Vercel CLI напрямую (не через npm script).

### 5. Деплой на Vercel

1. Подключите ваш репозиторий к Vercel
2. Добавьте переменную окружения `RETELL_API_KEY` в настройках проекта (Settings -> Environment Variables)
3. Деплой произойдет автоматически при push в репозиторий

## 📁 Структура проекта

```
.
├── api/
│   └── create-web-call.js    # Serverless функция для создания web call
├── index.html                 # Главная страница
├── package.json               # Зависимости проекта
├── .env.example              # Пример переменных окружения
├── .gitignore                # Игнорируемые файлы
└── README.md                 # Документация
```

## 🔧 API Endpoints

### POST `/api/create-web-call`

Создает web call через Retell AI API.

**Request Body:**
```json
{
  "agent_id": "agent_xxxxxxxxxxxxx"
}
```

**Response:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiJ9...",
  "call_id": "call_xxxxxxxxxxxxx"
}
```

## 📝 Использование

### Чат

Кнопка чата (нижний правый угол) открывает чат-виджет Retell AI.

### Голосовой звонок

Кнопка звонка (оранжевая, над кнопкой чата) инициирует web call с голосовым агентом.

**Требования:**
- Браузер должен поддерживать WebRTC
- Разрешите доступ к микрофону и динамикам
- Необходим голосовой агент в Retell AI

## 🔒 Безопасность

- API ключ Retell AI хранится только в переменных окружения на сервере
- Никогда не коммитьте `.env` файлы в репозиторий
- Используйте `.env.local` для локальной разработки

## 📚 Документация

- [Retell AI Documentation](https://docs.retellai.com)
- [Retell AI Dashboard](https://dashboard.retellai.com)
- [Vercel Documentation](https://vercel.com/docs)

## ⚠️ Важные заметки

1. **Голосовой агент vs Чат-агент:**
   - Для чата используйте **чат-агент** (Chat Agent)
   - Для звонков используйте **голосовой агент** (Voice Agent)
   - Это разные типы агентов!

2. **Retell Web SDK:**
   - SDK загружается динамически при первом использовании
   - Требуется стабильное интернет-соединение
   - Поддерживается в современных браузерах (Chrome, Firefox, Safari, Edge)

3. **Ограничения:**
   - Web calls требуют разрешения доступа к микрофону
   - Работают только в HTTPS окружении (или localhost)
   - Требуется стабильное соединение для качественной связи

## 🐛 Решение проблем

### Звонок не работает

1. Проверьте, что используете **голосовой агент**, а не чат-агент
2. Убедитесь, что `RETELL_API_KEY` установлен в Vercel
3. Проверьте консоль браузера на наличие ошибок
4. Убедитесь, что разрешили доступ к микрофону

### Чат не открывается

1. Проверьте `data-agent-id` в HTML - должен быть ID чат-агента
2. Проверьте `data-public-key` - должен быть правильный публичный ключ
3. Откройте консоль браузера для диагностики

## 📄 Лицензия

MIT
